export class TimeRecord {
  constructor(id, title, time) {
    this.id = id
    this.title = title
    this.time = time
  }
}

export class radioFormData {
  constructor(id, label) {
    this.id = id
    this.label = label
  }
}
